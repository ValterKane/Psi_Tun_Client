<Window x:Class="PsiTun.BootstrapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PsiTun Setup" Height="180" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="ToolWindow"
        Background="#1E1E2E" Foreground="#E0E0E0"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText" Grid.Row="0" Text="Setting up..."
                   FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <ProgressBar x:Name="ProgressBar" Grid.Row="1"
                     Height="8" Minimum="0" Maximum="100"
                     Margin="0,0,0,10"/>

        <TextBlock x:Name="DetailText" Grid.Row="2"
                   Text="" FontSize="12"
                   Foreground="#A0A0B0" TextWrapping="Wrap"/>

        <TextBlock x:Name="ErrorText" Grid.Row="3"
                   Text="" FontSize="12"
                   Foreground="#F44336" TextWrapping="Wrap"
                   Margin="0,8,0,0" Visibility="Collapsed"/>
    </Grid>
</Window>
