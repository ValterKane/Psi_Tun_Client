<Window x:Class="PsiTun.SourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Источник конфигурации" Height="210" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E2E" Foreground="#E0E0E0"
        FontFamily="Segoe UI" FontSize="13">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вставьте источник конфигурации:"
                   FontSize="13" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="1"
                   Text="Поддерживается URL подписки (например https://example.com/sub/...) и share-ссылка vless:// / vmess:// / trojan:// / ss://"
                   FontSize="11" Foreground="#A0A0B0" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <TextBox Grid.Row="2" x:Name="SourceBox"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Background="#2D2D3F" Foreground="#E0E0E0" CaretBrush="#E0E0E0"
                 BorderThickness="0" Padding="8,5" FontSize="12"
                 VerticalContentAlignment="Top"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Click="Ok_Click" IsDefault="True"
                    Width="90" Height="30" Margin="0,0,8,0"
                    Background="#5C6BC0" Foreground="White"
                    BorderThickness="0" Cursor="Hand" FontWeight="SemiBold"/>
            <Button Content="Отмена" IsCancel="True"
                    Width="90" Height="30"
                    Background="#555565" Foreground="#E0E0E0"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
