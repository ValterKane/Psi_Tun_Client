<Window x:Class="PsiTun.Views.RoutingRulesWindow"
        x:Name="window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Правила маршрутизации" Height="560" Width="880"
        MinHeight="400" MinWidth="720"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="#1E1E2E" Foreground="#E0E0E0"
        FontFamily="Segoe UI" FontSize="12">

    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252535"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#252535"/>
            <Setter Property="AlternatingRowBackground" Value="#2A2A3A"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#3D3D50"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionUnit" Value="Cell"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="#B0B0C0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1E1E2E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E2E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CaretBrush" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="RulesGrid" Grid.Row="0" Margin="0,0,0,10"
                  ItemsSource="{Binding Rules}"
                  AlternationCount="2" CanUserResizeRows="False"
                  RowHeight="32">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Вкл"
                    Binding="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}" Width="40"/>

                <DataGridComboBoxColumn Header="Тип"
                    SelectedItemBinding="{Binding MatchType, UpdateSourceTrigger=PropertyChanged}" Width="120"/>

                <DataGridTextColumn Header="Значение"
                    Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="*"/>

                <DataGridComboBoxColumn Header="Транспорт"
                    SelectedItemBinding="{Binding Network, UpdateSourceTrigger=PropertyChanged}" Width="80"/>

                <DataGridComboBoxColumn Header="Приложение"
                    SelectedItemBinding="{Binding AppProtocol, UpdateSourceTrigger=PropertyChanged}" Width="90"/>

                <DataGridTextColumn Header="Порт"
                    Binding="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Width="70">
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="#1E1E2E"/>
                            <Setter Property="Foreground" Value="#E0E0E0"/>
                            <Setter Property="CaretBrush" Value="#E0E0E0"/>
                            <EventSetter Event="PreviewTextInput" Handler="Port_PreviewTextInput"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>

                <DataGridComboBoxColumn Header="Действие"
                    SelectedItemBinding="{Binding Action, UpdateSourceTrigger=PropertyChanged}" Width="85"/>

                <DataGridCheckBoxColumn Header="Форс"
                    Binding="{Binding ForceProxy, UpdateSourceTrigger=PropertyChanged}" Width="44"/>

                <DataGridTemplateColumn Header="" Width="72">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="↑" Width="22" Height="22"
                                        Background="Transparent" Foreground="#B0B0C0"
                                        BorderThickness="0" FontSize="12"
                                        Command="{Binding DataContext.MoveUpCommand, Source={x:Reference window}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="↓" Width="22" Height="22"
                                        Background="Transparent" Foreground="#B0B0C0"
                                        BorderThickness="0" FontSize="12"
                                        Command="{Binding DataContext.MoveDownCommand, Source={x:Reference window}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="✕" Width="24" Height="24"
                                        Background="Transparent" Foreground="#F44336"
                                        BorderThickness="0" FontSize="14" FontWeight="Bold"
                                        Command="{Binding DataContext.DeleteRuleCommand, Source={x:Reference window}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="+ Добавить" Style="{StaticResource ActionButton}"
                    Background="#5C6BC0" Command="{Binding AddRuleCommand}" Margin="0,0,8,0"/>
            <Button Content="Сбросить" Style="{StaticResource ActionButton}"
                    Background="#455A64" Command="{Binding ResetDefaultsCommand}" Margin="0,0,8,0"/>
            <Button Content="Сохранить" Style="{StaticResource ActionButton}"
                    Background="#43A047" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
